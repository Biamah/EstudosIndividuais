// Faça um algoritmo para ler a base e a
// altura de um triângulo.  
// Em seguida, escreva a sua área. 
// Área = ( Base * Altura ) / 2  



const area = () => {
  let base = 10;
  let altura = 8;
  let calculaArea = (base * altura) / 2
  return calculaArea
}

console.log(area());
